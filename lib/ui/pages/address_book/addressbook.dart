import 'package:flutter/widgets.dart';
import 'package:flutter_wechat/ui/pages/address_book/addressbook_body.dart';

class AddressBookScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: AddressBookBody(),
    );
  }
}